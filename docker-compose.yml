version: "3"

services:
  main-db:
    build: ./db/main
    volumes:
      - ./db/main/initdb.d:/docker-entrypoint-initdb.d
    ports:
      - "3306:3306"
    environment:
      TZ: "Asia/Tokyo"
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: maindb
