// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodel

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// TenantTagType is an object representing the database table.
type TenantTagType struct {
	ID string `boil:"id" json:"id" toml:"id" yaml:"id"`

	R *tenantTagTypeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tenantTagTypeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TenantTagTypeColumns = struct {
	ID string
}{
	ID: "id",
}

var TenantTagTypeTableColumns = struct {
	ID string
}{
	ID: "tenant_tag_types.id",
}

// Generated where

var TenantTagTypeWhere = struct {
	ID whereHelperstring
}{
	ID: whereHelperstring{field: "\"tenant_tag_types\".\"id\""},
}

// TenantTagTypeRels is where relationship names are stored.
var TenantTagTypeRels = struct {
	TypeTenantTags string
}{
	TypeTenantTags: "TypeTenantTags",
}

// tenantTagTypeR is where relationships are stored.
type tenantTagTypeR struct {
	TypeTenantTags TenantTagSlice `boil:"TypeTenantTags" json:"TypeTenantTags" toml:"TypeTenantTags" yaml:"TypeTenantTags"`
}

// NewStruct creates a new relationship struct
func (*tenantTagTypeR) NewStruct() *tenantTagTypeR {
	return &tenantTagTypeR{}
}

func (o *TenantTagType) GetTypeTenantTags() TenantTagSlice {
	if o == nil {
		return nil
	}

	return o.R.GetTypeTenantTags()
}

func (r *tenantTagTypeR) GetTypeTenantTags() TenantTagSlice {
	if r == nil {
		return nil
	}

	return r.TypeTenantTags
}

// tenantTagTypeL is where Load methods for each relationship are stored.
type tenantTagTypeL struct{}

var (
	tenantTagTypeAllColumns            = []string{"id"}
	tenantTagTypeColumnsWithoutDefault = []string{"id"}
	tenantTagTypeColumnsWithDefault    = []string{}
	tenantTagTypePrimaryKeyColumns     = []string{"id"}
	tenantTagTypeGeneratedColumns      = []string{}
)

type (
	// TenantTagTypeSlice is an alias for a slice of pointers to TenantTagType.
	// This should almost always be used instead of []TenantTagType.
	TenantTagTypeSlice []*TenantTagType

	tenantTagTypeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tenantTagTypeType                 = reflect.TypeOf(&TenantTagType{})
	tenantTagTypeMapping              = queries.MakeStructMapping(tenantTagTypeType)
	tenantTagTypePrimaryKeyMapping, _ = queries.BindMapping(tenantTagTypeType, tenantTagTypeMapping, tenantTagTypePrimaryKeyColumns)
	tenantTagTypeInsertCacheMut       sync.RWMutex
	tenantTagTypeInsertCache          = make(map[string]insertCache)
	tenantTagTypeUpdateCacheMut       sync.RWMutex
	tenantTagTypeUpdateCache          = make(map[string]updateCache)
	tenantTagTypeUpsertCacheMut       sync.RWMutex
	tenantTagTypeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// OneG returns a single tenantTagType record from the query using the global executor.
func (q tenantTagTypeQuery) OneG(ctx context.Context) (*TenantTagType, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single tenantTagType record from the query using the global executor, and panics on error.
func (q tenantTagTypeQuery) OneGP(ctx context.Context) *TenantTagType {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single tenantTagType record from the query, and panics on error.
func (q tenantTagTypeQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *TenantTagType {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single tenantTagType record from the query.
func (q tenantTagTypeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TenantTagType, error) {
	o := &TenantTagType{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodel: failed to execute a one query for tenant_tag_types")
	}

	return o, nil
}

// AllG returns all TenantTagType records from the query using the global executor.
func (q tenantTagTypeQuery) AllG(ctx context.Context) (TenantTagTypeSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all TenantTagType records from the query using the global executor, and panics on error.
func (q tenantTagTypeQuery) AllGP(ctx context.Context) TenantTagTypeSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all TenantTagType records from the query, and panics on error.
func (q tenantTagTypeQuery) AllP(ctx context.Context, exec boil.ContextExecutor) TenantTagTypeSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all TenantTagType records from the query.
func (q tenantTagTypeQuery) All(ctx context.Context, exec boil.ContextExecutor) (TenantTagTypeSlice, error) {
	var o []*TenantTagType

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodel: failed to assign all query results to TenantTagType slice")
	}

	return o, nil
}

// CountG returns the count of all TenantTagType records in the query using the global executor
func (q tenantTagTypeQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all TenantTagType records in the query using the global executor, and panics on error.
func (q tenantTagTypeQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all TenantTagType records in the query, and panics on error.
func (q tenantTagTypeQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all TenantTagType records in the query.
func (q tenantTagTypeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to count tenant_tag_types rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q tenantTagTypeQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q tenantTagTypeQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q tenantTagTypeQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q tenantTagTypeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodel: failed to check if tenant_tag_types exists")
	}

	return count > 0, nil
}

// TypeTenantTags retrieves all the tenant_tag's TenantTags with an executor via type column.
func (o *TenantTagType) TypeTenantTags(mods ...qm.QueryMod) tenantTagQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"tenant_tags\".\"type\"=?", o.ID),
	)

	return TenantTags(queryMods...)
}

// LoadTypeTenantTags allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (tenantTagTypeL) LoadTypeTenantTags(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTenantTagType any, mods queries.Applicator) error {
	var slice []*TenantTagType
	var object *TenantTagType

	if singular {
		var ok bool
		object, ok = maybeTenantTagType.(*TenantTagType)
		if !ok {
			object = new(TenantTagType)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTenantTagType)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTenantTagType))
			}
		}
	} else {
		s, ok := maybeTenantTagType.(*[]*TenantTagType)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTenantTagType)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTenantTagType))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &tenantTagTypeR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tenantTagTypeR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`tenant_tags`),
		qm.WhereIn(`tenant_tags.type in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load tenant_tags")
	}

	var resultSlice []*TenantTag
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice tenant_tags")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on tenant_tags")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for tenant_tags")
	}

	if singular {
		object.R.TypeTenantTags = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &tenantTagR{}
			}
			foreign.R.TypeTenantTagType = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.Type {
				local.R.TypeTenantTags = append(local.R.TypeTenantTags, foreign)
				if foreign.R == nil {
					foreign.R = &tenantTagR{}
				}
				foreign.R.TypeTenantTagType = local
				break
			}
		}
	}

	return nil
}

// AddTypeTenantTagsG adds the given related objects to the existing relationships
// of the tenant_tag_type, optionally inserting them as new records.
// Appends related to o.R.TypeTenantTags.
// Sets related.R.TypeTenantTagType appropriately.
// Uses the global database handle.
func (o *TenantTagType) AddTypeTenantTagsG(ctx context.Context, insert bool, related ...*TenantTag) error {
	return o.AddTypeTenantTags(ctx, boil.GetContextDB(), insert, related...)
}

// AddTypeTenantTagsP adds the given related objects to the existing relationships
// of the tenant_tag_type, optionally inserting them as new records.
// Appends related to o.R.TypeTenantTags.
// Sets related.R.TypeTenantTagType appropriately.
// Panics on error.
func (o *TenantTagType) AddTypeTenantTagsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TenantTag) {
	if err := o.AddTypeTenantTags(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddTypeTenantTagsGP adds the given related objects to the existing relationships
// of the tenant_tag_type, optionally inserting them as new records.
// Appends related to o.R.TypeTenantTags.
// Sets related.R.TypeTenantTagType appropriately.
// Uses the global database handle and panics on error.
func (o *TenantTagType) AddTypeTenantTagsGP(ctx context.Context, insert bool, related ...*TenantTag) {
	if err := o.AddTypeTenantTags(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddTypeTenantTags adds the given related objects to the existing relationships
// of the tenant_tag_type, optionally inserting them as new records.
// Appends related to o.R.TypeTenantTags.
// Sets related.R.TypeTenantTagType appropriately.
func (o *TenantTagType) AddTypeTenantTags(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TenantTag) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.Type = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"tenant_tags\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"type"}),
				strmangle.WhereClause("\"", "\"", 2, tenantTagPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.Type = o.ID
		}
	}

	if o.R == nil {
		o.R = &tenantTagTypeR{
			TypeTenantTags: related,
		}
	} else {
		o.R.TypeTenantTags = append(o.R.TypeTenantTags, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &tenantTagR{
				TypeTenantTagType: o,
			}
		} else {
			rel.R.TypeTenantTagType = o
		}
	}
	return nil
}

// TenantTagTypes retrieves all the records using an executor.
func TenantTagTypes(mods ...qm.QueryMod) tenantTagTypeQuery {
	mods = append(mods, qm.From("\"tenant_tag_types\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"tenant_tag_types\".*"})
	}

	return tenantTagTypeQuery{q}
}

// FindTenantTagTypeG retrieves a single record by ID.
func FindTenantTagTypeG(ctx context.Context, iD string, selectCols ...string) (*TenantTagType, error) {
	return FindTenantTagType(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindTenantTagTypeP retrieves a single record by ID with an executor, and panics on error.
func FindTenantTagTypeP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *TenantTagType {
	retobj, err := FindTenantTagType(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTenantTagTypeGP retrieves a single record by ID, and panics on error.
func FindTenantTagTypeGP(ctx context.Context, iD string, selectCols ...string) *TenantTagType {
	retobj, err := FindTenantTagType(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTenantTagType retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTenantTagType(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*TenantTagType, error) {
	tenantTagTypeObj := &TenantTagType{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"tenant_tag_types\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, tenantTagTypeObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodel: unable to select from tenant_tag_types")
	}

	return tenantTagTypeObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TenantTagType) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *TenantTagType) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *TenantTagType) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TenantTagType) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodel: no tenant_tag_types provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(tenantTagTypeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tenantTagTypeInsertCacheMut.RLock()
	cache, cached := tenantTagTypeInsertCache[key]
	tenantTagTypeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tenantTagTypeAllColumns,
			tenantTagTypeColumnsWithDefault,
			tenantTagTypeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tenantTagTypeType, tenantTagTypeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tenantTagTypeType, tenantTagTypeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"tenant_tag_types\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"tenant_tag_types\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbmodel: unable to insert into tenant_tag_types")
	}

	if !cached {
		tenantTagTypeInsertCacheMut.Lock()
		tenantTagTypeInsertCache[key] = cache
		tenantTagTypeInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single TenantTagType record using the global executor.
// See Update for more documentation.
func (o *TenantTagType) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the TenantTagType, and panics on error.
// See Update for more documentation.
func (o *TenantTagType) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single TenantTagType record using the global executor. Panics on error.
// See Update for more documentation.
func (o *TenantTagType) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the TenantTagType.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TenantTagType) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	tenantTagTypeUpdateCacheMut.RLock()
	cache, cached := tenantTagTypeUpdateCache[key]
	tenantTagTypeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tenantTagTypeAllColumns,
			tenantTagTypePrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("dbmodel: unable to update tenant_tag_types, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"tenant_tag_types\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, tenantTagTypePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tenantTagTypeType, tenantTagTypeMapping, append(wl, tenantTagTypePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to update tenant_tag_types row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to get rows affected by update for tenant_tag_types")
	}

	if !cached {
		tenantTagTypeUpdateCacheMut.Lock()
		tenantTagTypeUpdateCache[key] = cache
		tenantTagTypeUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q tenantTagTypeQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q tenantTagTypeQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q tenantTagTypeQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q tenantTagTypeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to update all for tenant_tag_types")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to retrieve rows affected for tenant_tag_types")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TenantTagTypeSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o TenantTagTypeSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o TenantTagTypeSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TenantTagTypeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodel: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tenantTagTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"tenant_tag_types\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, tenantTagTypePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to update all in tenantTagType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to retrieve rows affected all in update all tenantTagType")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TenantTagType) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *TenantTagType) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *TenantTagType) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TenantTagType) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbmodel: no tenant_tag_types provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(tenantTagTypeColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tenantTagTypeUpsertCacheMut.RLock()
	cache, cached := tenantTagTypeUpsertCache[key]
	tenantTagTypeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tenantTagTypeAllColumns,
			tenantTagTypeColumnsWithDefault,
			tenantTagTypeColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tenantTagTypeAllColumns,
			tenantTagTypePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbmodel: unable to upsert tenant_tag_types, could not build update column list")
		}

		ret := strmangle.SetComplement(tenantTagTypeAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(tenantTagTypePrimaryKeyColumns) == 0 {
				return errors.New("dbmodel: unable to upsert tenant_tag_types, could not build conflict column list")
			}

			conflict = make([]string, len(tenantTagTypePrimaryKeyColumns))
			copy(conflict, tenantTagTypePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"tenant_tag_types\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(tenantTagTypeType, tenantTagTypeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tenantTagTypeType, tenantTagTypeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbmodel: unable to upsert tenant_tag_types")
	}

	if !cached {
		tenantTagTypeUpsertCacheMut.Lock()
		tenantTagTypeUpsertCache[key] = cache
		tenantTagTypeUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteG deletes a single TenantTagType record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TenantTagType) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// DeleteP deletes a single TenantTagType record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TenantTagType) DeleteP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.Delete(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single TenantTagType record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TenantTagType) DeleteGP(ctx context.Context) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single TenantTagType record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TenantTagType) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodel: no TenantTagType provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tenantTagTypePrimaryKeyMapping)
	sql := "DELETE FROM \"tenant_tag_types\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to delete from tenant_tag_types")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to get rows affected by delete for tenant_tag_types")
	}

	return rowsAff, nil
}

func (q tenantTagTypeQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows, and panics on error.
func (q tenantTagTypeQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q tenantTagTypeQuery) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q tenantTagTypeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodel: no tenantTagTypeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to delete all from tenant_tag_types")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to get rows affected by deleteall for tenant_tag_types")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TenantTagTypeSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o TenantTagTypeSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o TenantTagTypeSlice) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TenantTagTypeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tenantTagTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"tenant_tag_types\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, tenantTagTypePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to delete all from tenantTagType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to get rows affected by deleteall for tenant_tag_types")
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TenantTagType) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("dbmodel: no TenantTagType provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *TenantTagType) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *TenantTagType) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TenantTagType) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTenantTagType(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TenantTagTypeSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("dbmodel: empty TenantTagTypeSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TenantTagTypeSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TenantTagTypeSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TenantTagTypeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TenantTagTypeSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tenantTagTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"tenant_tag_types\".* FROM \"tenant_tag_types\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, tenantTagTypePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodel: unable to reload all in TenantTagTypeSlice")
	}

	*o = slice

	return nil
}

// TenantTagTypeExistsG checks if the TenantTagType row exists.
func TenantTagTypeExistsG(ctx context.Context, iD string) (bool, error) {
	return TenantTagTypeExists(ctx, boil.GetContextDB(), iD)
}

// TenantTagTypeExistsP checks if the TenantTagType row exists. Panics on error.
func TenantTagTypeExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := TenantTagTypeExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TenantTagTypeExistsGP checks if the TenantTagType row exists. Panics on error.
func TenantTagTypeExistsGP(ctx context.Context, iD string) bool {
	e, err := TenantTagTypeExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TenantTagTypeExists checks if the TenantTagType row exists.
func TenantTagTypeExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"tenant_tag_types\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodel: unable to check if tenant_tag_types exists")
	}

	return exists, nil
}

// Exists checks if the TenantTagType row exists.
func (o *TenantTagType) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TenantTagTypeExists(ctx, exec, o.ID)
}

// InsertAll inserts all rows with the specified column values, using an executor.
// IMPORTANT: this will calculate the widest columns from all items in the slice, be careful if you want to use default column values
func (o TenantTagTypeSlice) InsertAll(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	// Calculate the widest columns from all rows need to insert
	wlCols := make(map[string]struct{}, 10)
	for _, row := range o {
		wl, _ := columns.InsertColumnSet(
			tenantTagTypeAllColumns,
			tenantTagTypeColumnsWithDefault,
			tenantTagTypeColumnsWithoutDefault,
			queries.NonZeroDefaultSet(tenantTagTypeColumnsWithDefault, row),
		)
		for _, col := range wl {
			wlCols[col] = struct{}{}
		}
		if len(wlCols) == len(tenantTagTypeAllColumns) {
			break
		}
	}
	wl := make([]string, 0, len(wlCols))
	for _, col := range tenantTagTypeAllColumns {
		if _, ok := wlCols[col]; ok {
			wl = append(wl, col)
		}
	}

	var sql string
	vals := []interface{}{}
	for i, row := range o {

		if i == 0 {
			sql = "INSERT INTO \"tenant_tag_types\" " + "(\"" + strings.Join(wl, "\",\"") + "\")" + " VALUES "
		}
		sql += strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), len(vals)+1, len(wl))
		if i != len(o)-1 {
			sql += ","
		}
		valMapping, err := queries.BindMapping(tenantTagTypeType, tenantTagTypeMapping, wl)
		if err != nil {
			return 0, err
		}

		value := reflect.Indirect(reflect.ValueOf(row))
		vals = append(vals, queries.ValuesFromMapping(value, valMapping)...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, vals)
	}

	result, err := exec.ExecContext(ctx, sql, vals...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to insert all from tenantTagType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to get rows affected by insertall for tenant_tag_types")
	}

	return rowsAff, nil
}

// InsertIgnoreAll inserts all rows with ignoring the existing ones having the same primary key values.
// IMPORTANT: this will calculate the widest columns from all items in the slice, be careful if you want to use default column values
func (o TenantTagTypeSlice) InsertIgnoreAll(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	return o.UpsertAll(ctx, exec, boil.None(), columns)
}

// UpsertAll inserts or updates all rows.
// Currently it doesn't support "NoContext" and "NoRowsAffected".
// IMPORTANT: this will calculate the widest columns from all items in the slice, be careful if you want to use default column values.
// IMPORTANT: any AUTO_INCREMENT column should be excluded from `updateColumns` and `insertColumns` including PK.
func (o TenantTagTypeSlice) UpsertAll(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) (int64, error) {
	return o.upsertAllOnConflictColumns(ctx, exec, nil, updateColumns, insertColumns)
}

// upsertAllOnConflictColumns upserts multiple rows with passing custom conflict columns to allow bypassing
// single column conflict check (see bug https://github.com/aarondl/sqlboiler/issues/328).
// SQLBoiler only checks column conflict on a single column which is insufficient when a UNIQUE index
// spans multiple columns.
// This function allows passing multiple conflict columns, but it cannot check whether they are correct or not.
// So use it at your own risk.
func (o TenantTagTypeSlice) UpsertAllOnConflictColumns(ctx context.Context, exec boil.ContextExecutor, conflictColumns []string, updateColumns, insertColumns boil.Columns) (int64, error) {
	return o.upsertAllOnConflictColumns(ctx, exec, conflictColumns, updateColumns, insertColumns)
}

func (o TenantTagTypeSlice) upsertAllOnConflictColumns(ctx context.Context, exec boil.ContextExecutor, conflictColumns []string, updateColumns, insertColumns boil.Columns) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	// Calculate the widest columns from all rows need to upsert
	insertCols := make(map[string]struct{}, 10)
	for _, row := range o {
		insert, _ := insertColumns.InsertColumnSet(
			tenantTagTypeAllColumns,
			tenantTagTypeColumnsWithDefault,
			tenantTagTypeColumnsWithoutDefault,
			queries.NonZeroDefaultSet(tenantTagTypeColumnsWithDefault, row),
		)
		for _, col := range insert {
			insertCols[col] = struct{}{}
		}
		if len(insertCols) == len(tenantTagTypeAllColumns) || (insertColumns.IsWhitelist() && len(insertCols) == len(insertColumns.Cols)) {
			break
		}
	}
	insert := make([]string, 0, len(insertCols))
	for _, col := range tenantTagTypeAllColumns {
		if _, ok := insertCols[col]; ok {
			insert = append(insert, col)
		}
	}
	if len(insert) == 0 {
		return 0, errors.New("dbmodel: unable to upsert tenant_tag_types, could not build insert column list")
	}

	update := updateColumns.UpdateColumnSet(
		tenantTagTypeAllColumns,
		tenantTagTypePrimaryKeyColumns,
	)

	updateRequired := !updateColumns.IsNone() && len(update) != 0
	if !updateColumns.IsNone() && len(update) == 0 {
		return 0, errors.New("dbmodel: unable to upsert tenant_tag_types, could not build update column list")
	}

	conflict := conflictColumns
	if len(conflict) == 0 && updateRequired {
		conflict = make([]string, len(tenantTagTypePrimaryKeyColumns))
		copy(conflict, tenantTagTypePrimaryKeyColumns)
	}
	if updateRequired && len(conflict) == 0 {
		return 0, errors.New("dbmodel: unable to upsert tenant_tag_types, could not build conflict column list")
	}

	quotedInsert := strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, insert)
	placeholders := strmangle.Placeholders(dialect.UseIndexPlaceholders, len(insert)*len(o), 1, len(insert))

	buf := strmangle.GetBuffer()
	defer strmangle.PutBuffer(buf)

	fmt.Fprintf(
		buf,
		"INSERT INTO \"tenant_tag_types\"(%s) VALUES %s",
		strings.Join(quotedInsert, ","),
		placeholders,
	)

	buf.WriteString(" ON CONFLICT")
	if len(conflict) != 0 {
		quotedConflict := strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, conflict)
		buf.WriteString(" (")
		buf.WriteString(strings.Join(quotedConflict, ","))
		buf.WriteString(")")
	}
	buf.WriteByte(' ')

	if !updateRequired {
		buf.WriteString("DO NOTHING")
	} else {
		buf.WriteString("DO UPDATE SET ")
		for i, col := range update {
			if i != 0 {
				buf.WriteByte(',')
			}
			quoted := strmangle.IdentQuote(dialect.LQ, dialect.RQ, col)
			buf.WriteString(quoted)
			buf.WriteString(" = EXCLUDED.")
			buf.WriteString(quoted)
		}
	}

	query := buf.String()
	valueMapping, err := queries.BindMapping(tenantTagTypeType, tenantTagTypeMapping, insert)
	if err != nil {
		return 0, err
	}

	var vals []interface{}
	for _, row := range o {

		value := reflect.Indirect(reflect.ValueOf(row))
		vals = append(vals, queries.ValuesFromMapping(value, valueMapping)...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, vals)
	}

	result, err := exec.ExecContext(ctx, query, vals...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to upsert for tenant_tag_types")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to get rows affected by upsert for tenant_tag_types")
	}

	return rowsAff, nil
}
