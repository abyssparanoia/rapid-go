// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodel

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// TenantTag is an object representing the database table.
type TenantTag struct {
	// id
	ID string `boil:"id" json:"id" toml:"id" yaml:"id"`
	// tenant_id
	TenantID string `boil:"tenant_id" json:"tenant_id" toml:"tenant_id" yaml:"tenant_id"`
	// type
	Type string `boil:"type" json:"type" toml:"type" yaml:"type"`
	// created date
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	// update date
	UpdatedAt time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *tenantTagR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tenantTagL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TenantTagColumns = struct {
	ID        string
	TenantID  string
	Type      string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "id",
	TenantID:  "tenant_id",
	Type:      "type",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
}

var TenantTagTableColumns = struct {
	ID        string
	TenantID  string
	Type      string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "tenant_tags.id",
	TenantID:  "tenant_tags.tenant_id",
	Type:      "tenant_tags.type",
	CreatedAt: "tenant_tags.created_at",
	UpdatedAt: "tenant_tags.updated_at",
}

// Generated where

var TenantTagWhere = struct {
	ID        whereHelperstring
	TenantID  whereHelperstring
	Type      whereHelperstring
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
}{
	ID:        whereHelperstring{field: "`tenant_tags`.`id`"},
	TenantID:  whereHelperstring{field: "`tenant_tags`.`tenant_id`"},
	Type:      whereHelperstring{field: "`tenant_tags`.`type`"},
	CreatedAt: whereHelpertime_Time{field: "`tenant_tags`.`created_at`"},
	UpdatedAt: whereHelpertime_Time{field: "`tenant_tags`.`updated_at`"},
}

// TenantTagRels is where relationship names are stored.
var TenantTagRels = struct {
	Tenant            string
	TypeTenantTagType string
}{
	Tenant:            "Tenant",
	TypeTenantTagType: "TypeTenantTagType",
}

// tenantTagR is where relationships are stored.
type tenantTagR struct {
	Tenant            *Tenant        `boil:"Tenant" json:"Tenant" toml:"Tenant" yaml:"Tenant"`
	TypeTenantTagType *TenantTagType `boil:"TypeTenantTagType" json:"TypeTenantTagType" toml:"TypeTenantTagType" yaml:"TypeTenantTagType"`
}

// NewStruct creates a new relationship struct
func (*tenantTagR) NewStruct() *tenantTagR {
	return &tenantTagR{}
}

func (o *TenantTag) GetTenant() *Tenant {
	if o == nil {
		return nil
	}

	return o.R.GetTenant()
}

func (r *tenantTagR) GetTenant() *Tenant {
	if r == nil {
		return nil
	}

	return r.Tenant
}

func (o *TenantTag) GetTypeTenantTagType() *TenantTagType {
	if o == nil {
		return nil
	}

	return o.R.GetTypeTenantTagType()
}

func (r *tenantTagR) GetTypeTenantTagType() *TenantTagType {
	if r == nil {
		return nil
	}

	return r.TypeTenantTagType
}

// tenantTagL is where Load methods for each relationship are stored.
type tenantTagL struct{}

var (
	tenantTagAllColumns            = []string{"id", "tenant_id", "type", "created_at", "updated_at"}
	tenantTagColumnsWithoutDefault = []string{"id", "tenant_id", "type", "created_at", "updated_at"}
	tenantTagColumnsWithDefault    = []string{}
	tenantTagPrimaryKeyColumns     = []string{"id"}
	tenantTagGeneratedColumns      = []string{}
)

type (
	// TenantTagSlice is an alias for a slice of pointers to TenantTag.
	// This should almost always be used instead of []TenantTag.
	TenantTagSlice []*TenantTag

	tenantTagQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tenantTagType                 = reflect.TypeOf(&TenantTag{})
	tenantTagMapping              = queries.MakeStructMapping(tenantTagType)
	tenantTagPrimaryKeyMapping, _ = queries.BindMapping(tenantTagType, tenantTagMapping, tenantTagPrimaryKeyColumns)
	tenantTagInsertCacheMut       sync.RWMutex
	tenantTagInsertCache          = make(map[string]insertCache)
	tenantTagUpdateCacheMut       sync.RWMutex
	tenantTagUpdateCache          = make(map[string]updateCache)
	tenantTagUpsertCacheMut       sync.RWMutex
	tenantTagUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// OneG returns a single tenantTag record from the query using the global executor.
func (q tenantTagQuery) OneG(ctx context.Context) (*TenantTag, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single tenantTag record from the query using the global executor, and panics on error.
func (q tenantTagQuery) OneGP(ctx context.Context) *TenantTag {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single tenantTag record from the query, and panics on error.
func (q tenantTagQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *TenantTag {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single tenantTag record from the query.
func (q tenantTagQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TenantTag, error) {
	o := &TenantTag{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodel: failed to execute a one query for tenant_tags")
	}

	return o, nil
}

// AllG returns all TenantTag records from the query using the global executor.
func (q tenantTagQuery) AllG(ctx context.Context) (TenantTagSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all TenantTag records from the query using the global executor, and panics on error.
func (q tenantTagQuery) AllGP(ctx context.Context) TenantTagSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all TenantTag records from the query, and panics on error.
func (q tenantTagQuery) AllP(ctx context.Context, exec boil.ContextExecutor) TenantTagSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all TenantTag records from the query.
func (q tenantTagQuery) All(ctx context.Context, exec boil.ContextExecutor) (TenantTagSlice, error) {
	var o []*TenantTag

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodel: failed to assign all query results to TenantTag slice")
	}

	return o, nil
}

// CountG returns the count of all TenantTag records in the query using the global executor
func (q tenantTagQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all TenantTag records in the query using the global executor, and panics on error.
func (q tenantTagQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all TenantTag records in the query, and panics on error.
func (q tenantTagQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all TenantTag records in the query.
func (q tenantTagQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to count tenant_tags rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q tenantTagQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q tenantTagQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q tenantTagQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q tenantTagQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodel: failed to check if tenant_tags exists")
	}

	return count > 0, nil
}

// Tenant pointed to by the foreign key.
func (o *TenantTag) Tenant(mods ...qm.QueryMod) tenantQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`id` = ?", o.TenantID),
	}

	queryMods = append(queryMods, mods...)

	return Tenants(queryMods...)
}

// TypeTenantTagType pointed to by the foreign key.
func (o *TenantTag) TypeTenantTagType(mods ...qm.QueryMod) tenantTagTypeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`id` = ?", o.Type),
	}

	queryMods = append(queryMods, mods...)

	return TenantTagTypes(queryMods...)
}

// LoadTenant allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tenantTagL) LoadTenant(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTenantTag any, mods queries.Applicator) error {
	var slice []*TenantTag
	var object *TenantTag

	if singular {
		var ok bool
		object, ok = maybeTenantTag.(*TenantTag)
		if !ok {
			object = new(TenantTag)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTenantTag)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTenantTag))
			}
		}
	} else {
		s, ok := maybeTenantTag.(*[]*TenantTag)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTenantTag)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTenantTag))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &tenantTagR{}
		}
		args[object.TenantID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tenantTagR{}
			}

			args[obj.TenantID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`tenants`),
		qm.WhereIn(`tenants.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Tenant")
	}

	var resultSlice []*Tenant
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Tenant")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for tenants")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for tenants")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Tenant = foreign
		if foreign.R == nil {
			foreign.R = &tenantR{}
		}
		foreign.R.TenantTags = append(foreign.R.TenantTags, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TenantID == foreign.ID {
				local.R.Tenant = foreign
				if foreign.R == nil {
					foreign.R = &tenantR{}
				}
				foreign.R.TenantTags = append(foreign.R.TenantTags, local)
				break
			}
		}
	}

	return nil
}

// LoadTypeTenantTagType allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tenantTagL) LoadTypeTenantTagType(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTenantTag any, mods queries.Applicator) error {
	var slice []*TenantTag
	var object *TenantTag

	if singular {
		var ok bool
		object, ok = maybeTenantTag.(*TenantTag)
		if !ok {
			object = new(TenantTag)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTenantTag)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTenantTag))
			}
		}
	} else {
		s, ok := maybeTenantTag.(*[]*TenantTag)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTenantTag)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTenantTag))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &tenantTagR{}
		}
		args[object.Type] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tenantTagR{}
			}

			args[obj.Type] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`tenant_tag_types`),
		qm.WhereIn(`tenant_tag_types.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load TenantTagType")
	}

	var resultSlice []*TenantTagType
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice TenantTagType")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for tenant_tag_types")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for tenant_tag_types")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.TypeTenantTagType = foreign
		if foreign.R == nil {
			foreign.R = &tenantTagTypeR{}
		}
		foreign.R.TypeTenantTags = append(foreign.R.TypeTenantTags, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.Type == foreign.ID {
				local.R.TypeTenantTagType = foreign
				if foreign.R == nil {
					foreign.R = &tenantTagTypeR{}
				}
				foreign.R.TypeTenantTags = append(foreign.R.TypeTenantTags, local)
				break
			}
		}
	}

	return nil
}

// SetTenantG of the tenantTag to the related item.
// Sets o.R.Tenant to related.
// Adds o to related.R.TenantTags.
// Uses the global database handle.
func (o *TenantTag) SetTenantG(ctx context.Context, insert bool, related *Tenant) error {
	return o.SetTenant(ctx, boil.GetContextDB(), insert, related)
}

// SetTenantP of the tenantTag to the related item.
// Sets o.R.Tenant to related.
// Adds o to related.R.TenantTags.
// Panics on error.
func (o *TenantTag) SetTenantP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Tenant) {
	if err := o.SetTenant(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetTenantGP of the tenantTag to the related item.
// Sets o.R.Tenant to related.
// Adds o to related.R.TenantTags.
// Uses the global database handle and panics on error.
func (o *TenantTag) SetTenantGP(ctx context.Context, insert bool, related *Tenant) {
	if err := o.SetTenant(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetTenant of the tenantTag to the related item.
// Sets o.R.Tenant to related.
// Adds o to related.R.TenantTags.
func (o *TenantTag) SetTenant(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Tenant) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `tenant_tags` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"tenant_id"}),
		strmangle.WhereClause("`", "`", 0, tenantTagPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TenantID = related.ID
	if o.R == nil {
		o.R = &tenantTagR{
			Tenant: related,
		}
	} else {
		o.R.Tenant = related
	}

	if related.R == nil {
		related.R = &tenantR{
			TenantTags: TenantTagSlice{o},
		}
	} else {
		related.R.TenantTags = append(related.R.TenantTags, o)
	}

	return nil
}

// SetTypeTenantTagTypeG of the tenantTag to the related item.
// Sets o.R.TypeTenantTagType to related.
// Adds o to related.R.TypeTenantTags.
// Uses the global database handle.
func (o *TenantTag) SetTypeTenantTagTypeG(ctx context.Context, insert bool, related *TenantTagType) error {
	return o.SetTypeTenantTagType(ctx, boil.GetContextDB(), insert, related)
}

// SetTypeTenantTagTypeP of the tenantTag to the related item.
// Sets o.R.TypeTenantTagType to related.
// Adds o to related.R.TypeTenantTags.
// Panics on error.
func (o *TenantTag) SetTypeTenantTagTypeP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *TenantTagType) {
	if err := o.SetTypeTenantTagType(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetTypeTenantTagTypeGP of the tenantTag to the related item.
// Sets o.R.TypeTenantTagType to related.
// Adds o to related.R.TypeTenantTags.
// Uses the global database handle and panics on error.
func (o *TenantTag) SetTypeTenantTagTypeGP(ctx context.Context, insert bool, related *TenantTagType) {
	if err := o.SetTypeTenantTagType(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetTypeTenantTagType of the tenantTag to the related item.
// Sets o.R.TypeTenantTagType to related.
// Adds o to related.R.TypeTenantTags.
func (o *TenantTag) SetTypeTenantTagType(ctx context.Context, exec boil.ContextExecutor, insert bool, related *TenantTagType) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `tenant_tags` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"type"}),
		strmangle.WhereClause("`", "`", 0, tenantTagPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.Type = related.ID
	if o.R == nil {
		o.R = &tenantTagR{
			TypeTenantTagType: related,
		}
	} else {
		o.R.TypeTenantTagType = related
	}

	if related.R == nil {
		related.R = &tenantTagTypeR{
			TypeTenantTags: TenantTagSlice{o},
		}
	} else {
		related.R.TypeTenantTags = append(related.R.TypeTenantTags, o)
	}

	return nil
}

// TenantTags retrieves all the records using an executor.
func TenantTags(mods ...qm.QueryMod) tenantTagQuery {
	mods = append(mods, qm.From("`tenant_tags`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`tenant_tags`.*"})
	}

	return tenantTagQuery{q}
}

// FindTenantTagG retrieves a single record by ID.
func FindTenantTagG(ctx context.Context, iD string, selectCols ...string) (*TenantTag, error) {
	return FindTenantTag(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindTenantTagP retrieves a single record by ID with an executor, and panics on error.
func FindTenantTagP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *TenantTag {
	retobj, err := FindTenantTag(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTenantTagGP retrieves a single record by ID, and panics on error.
func FindTenantTagGP(ctx context.Context, iD string, selectCols ...string) *TenantTag {
	retobj, err := FindTenantTag(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTenantTag retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTenantTag(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*TenantTag, error) {
	tenantTagObj := &TenantTag{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `tenant_tags` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, tenantTagObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodel: unable to select from tenant_tags")
	}

	return tenantTagObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TenantTag) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *TenantTag) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *TenantTag) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TenantTag) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodel: no tenant_tags provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(tenantTagColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tenantTagInsertCacheMut.RLock()
	cache, cached := tenantTagInsertCache[key]
	tenantTagInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tenantTagAllColumns,
			tenantTagColumnsWithDefault,
			tenantTagColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tenantTagType, tenantTagMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tenantTagType, tenantTagMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `tenant_tags` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `tenant_tags` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `tenant_tags` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, tenantTagPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "dbmodel: unable to insert into tenant_tags")
	}

	var identifierCols []any

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []any{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "dbmodel: unable to populate default values for tenant_tags")
	}

CacheNoHooks:
	if !cached {
		tenantTagInsertCacheMut.Lock()
		tenantTagInsertCache[key] = cache
		tenantTagInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single TenantTag record using the global executor.
// See Update for more documentation.
func (o *TenantTag) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the TenantTag, and panics on error.
// See Update for more documentation.
func (o *TenantTag) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single TenantTag record using the global executor. Panics on error.
// See Update for more documentation.
func (o *TenantTag) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the TenantTag.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TenantTag) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	tenantTagUpdateCacheMut.RLock()
	cache, cached := tenantTagUpdateCache[key]
	tenantTagUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tenantTagAllColumns,
			tenantTagPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("dbmodel: unable to update tenant_tags, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `tenant_tags` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, tenantTagPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tenantTagType, tenantTagMapping, append(wl, tenantTagPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to update tenant_tags row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to get rows affected by update for tenant_tags")
	}

	if !cached {
		tenantTagUpdateCacheMut.Lock()
		tenantTagUpdateCache[key] = cache
		tenantTagUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q tenantTagQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q tenantTagQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q tenantTagQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q tenantTagQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to update all for tenant_tags")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to retrieve rows affected for tenant_tags")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TenantTagSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o TenantTagSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o TenantTagSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TenantTagSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodel: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tenantTagPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `tenant_tags` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tenantTagPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to update all in tenantTag slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to retrieve rows affected all in update all tenantTag")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TenantTag) UpsertG(ctx context.Context, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateColumns, insertColumns)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *TenantTag) UpsertGP(ctx context.Context, updateColumns, insertColumns boil.Columns) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateColumns, insertColumns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *TenantTag) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) {
	if err := o.Upsert(ctx, exec, updateColumns, insertColumns); err != nil {
		panic(boil.WrapErr(err))
	}
}

var mySQLTenantTagUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TenantTag) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodel: no tenant_tags provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(tenantTagColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTenantTagUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tenantTagUpsertCacheMut.RLock()
	cache, cached := tenantTagUpsertCache[key]
	tenantTagUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tenantTagAllColumns,
			tenantTagColumnsWithDefault,
			tenantTagColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tenantTagAllColumns,
			tenantTagPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("dbmodel: unable to upsert tenant_tags, could not build update column list")
		}

		ret := strmangle.SetComplement(tenantTagAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`tenant_tags`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `tenant_tags` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(tenantTagType, tenantTagMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tenantTagType, tenantTagMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "dbmodel: unable to upsert for tenant_tags")
	}

	var uniqueMap []uint64
	var nzUniqueCols []any

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(tenantTagType, tenantTagMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "dbmodel: unable to retrieve unique values for tenant_tags")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "dbmodel: unable to populate default values for tenant_tags")
	}

CacheNoHooks:
	if !cached {
		tenantTagUpsertCacheMut.Lock()
		tenantTagUpsertCache[key] = cache
		tenantTagUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteG deletes a single TenantTag record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TenantTag) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// DeleteP deletes a single TenantTag record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TenantTag) DeleteP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.Delete(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single TenantTag record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TenantTag) DeleteGP(ctx context.Context) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single TenantTag record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TenantTag) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodel: no TenantTag provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tenantTagPrimaryKeyMapping)
	sql := "DELETE FROM `tenant_tags` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to delete from tenant_tags")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to get rows affected by delete for tenant_tags")
	}

	return rowsAff, nil
}

func (q tenantTagQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows, and panics on error.
func (q tenantTagQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q tenantTagQuery) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q tenantTagQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodel: no tenantTagQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to delete all from tenant_tags")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to get rows affected by deleteall for tenant_tags")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TenantTagSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o TenantTagSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o TenantTagSlice) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TenantTagSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tenantTagPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `tenant_tags` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tenantTagPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to delete all from tenantTag slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to get rows affected by deleteall for tenant_tags")
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TenantTag) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("dbmodel: no TenantTag provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *TenantTag) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *TenantTag) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TenantTag) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTenantTag(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TenantTagSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("dbmodel: empty TenantTagSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TenantTagSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TenantTagSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TenantTagSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TenantTagSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tenantTagPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `tenant_tags`.* FROM `tenant_tags` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tenantTagPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodel: unable to reload all in TenantTagSlice")
	}

	*o = slice

	return nil
}

// TenantTagExistsG checks if the TenantTag row exists.
func TenantTagExistsG(ctx context.Context, iD string) (bool, error) {
	return TenantTagExists(ctx, boil.GetContextDB(), iD)
}

// TenantTagExistsP checks if the TenantTag row exists. Panics on error.
func TenantTagExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := TenantTagExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TenantTagExistsGP checks if the TenantTag row exists. Panics on error.
func TenantTagExistsGP(ctx context.Context, iD string) bool {
	e, err := TenantTagExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TenantTagExists checks if the TenantTag row exists.
func TenantTagExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `tenant_tags` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodel: unable to check if tenant_tags exists")
	}

	return exists, nil
}

// Exists checks if the TenantTag row exists.
func (o *TenantTag) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TenantTagExists(ctx, exec, o.ID)
}

// InsertAll inserts all rows with the specified column values, using an executor.
// IMPORTANT: this will calculate the widest columns from all items in the slice, be careful if you want to use default column values
func (o TenantTagSlice) InsertAll(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	// Calculate the widest columns from all rows need to insert
	wlCols := make(map[string]struct{}, 10)
	for _, row := range o {
		wl, _ := columns.InsertColumnSet(
			tenantTagAllColumns,
			tenantTagColumnsWithDefault,
			tenantTagColumnsWithoutDefault,
			queries.NonZeroDefaultSet(tenantTagColumnsWithDefault, row),
		)
		for _, col := range wl {
			wlCols[col] = struct{}{}
		}
		if len(wlCols) == len(tenantTagAllColumns) {
			break
		}
	}
	wl := make([]string, 0, len(wlCols))
	for _, col := range tenantTagAllColumns {
		if _, ok := wlCols[col]; ok {
			wl = append(wl, col)
		}
	}

	var sql string
	vals := []interface{}{}
	for i, row := range o {

		if i == 0 {
			sql = "INSERT INTO `tenant_tags` " + "(`" + strings.Join(wl, "`,`") + "`)" + " VALUES "
		}
		sql += strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), len(vals)+1, len(wl))
		if i != len(o)-1 {
			sql += ","
		}
		valMapping, err := queries.BindMapping(tenantTagType, tenantTagMapping, wl)
		if err != nil {
			return 0, err
		}

		value := reflect.Indirect(reflect.ValueOf(row))
		vals = append(vals, queries.ValuesFromMapping(value, valMapping)...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, vals)
	}

	result, err := exec.ExecContext(ctx, sql, vals...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to insert all from tenantTag slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to get rows affected by insertall for tenant_tags")
	}

	return rowsAff, nil
}

// InsertIgnoreAll inserts all rows with ignoring the existing ones having the same primary key values.
// IMPORTANT: this will calculate the widest columns from all items in the slice, be careful if you want to use default column values
func (o TenantTagSlice) InsertIgnoreAll(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	return o.UpsertAll(ctx, exec, boil.None(), columns)
}

// UpsertAll inserts or updates all rows.
// Currently it doesn't support "NoContext" and "NoRowsAffected".
// IMPORTANT: this will calculate the widest columns from all items in the slice, be careful if you want to use default column values.
// IMPORTANT: any AUTO_INCREMENT column should be excluded from `updateColumns` and `insertColumns` including PK.
func (o TenantTagSlice) UpsertAll(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) (int64, error) {
	return o.upsertAllOnConflictColumns(ctx, exec, nil, updateColumns, insertColumns)
}

// upsertAllOnConflictColumns upserts multiple rows with passing custom conflict columns to allow bypassing
// single column conflict check (see bug https://github.com/aarondl/sqlboiler/issues/328).
// SQLBoiler only checks column conflict on single column only which is not correct as MySQL PK or UNIQUE index
// can include multiple columns.
// This function allows passing multiple conflict columns, but it cannot check whether they are correct or not.
// So use it at your own risk.
func (o TenantTagSlice) UpsertAllOnConflictColumns(ctx context.Context, exec boil.ContextExecutor, conflictColumns []string, updateColumns, insertColumns boil.Columns) (int64, error) {
	return o.upsertAllOnConflictColumns(ctx, exec, conflictColumns, updateColumns, insertColumns)
}

func (o TenantTagSlice) upsertAllOnConflictColumns(ctx context.Context, exec boil.ContextExecutor, conflictColumns []string, updateColumns, insertColumns boil.Columns) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	checkNZUniques := len(conflictColumns) == 0
	if len(conflictColumns) > 0 {
		mapConflictColumns := make(map[string]struct{}, len(conflictColumns))
		for _, col := range conflictColumns {
			for _, existCol := range tenantTagAllColumns {
				if col == existCol {
					mapConflictColumns[col] = struct{}{}
					break
				}
			}
		}
		if len(mapConflictColumns) <= 1 {
			return 0, errors.New("custom conflict columns must be 2 columns or more")
		}
	}

	// Calculate the widest columns from all rows need to upsert
	insertCols := make(map[string]struct{}, 10)
	for _, row := range o {
		if checkNZUniques {
			nzUniques := queries.NonZeroDefaultSet(mySQLTenantTagUniqueColumns, row)
			if len(nzUniques) == 0 {
				return 0, errors.New("cannot upsert with a table that cannot conflict on a unique column")
			}
		}
		insert, _ := insertColumns.InsertColumnSet(
			tenantTagAllColumns,
			tenantTagColumnsWithDefault,
			tenantTagColumnsWithoutDefault,
			queries.NonZeroDefaultSet(tenantTagColumnsWithDefault, row),
		)
		for _, col := range insert {
			insertCols[col] = struct{}{}
		}
		if len(insertCols) == len(tenantTagAllColumns) || (insertColumns.IsWhitelist() && len(insertCols) == len(insertColumns.Cols)) {
			break
		}
	}
	insert := make([]string, 0, len(insertCols))
	for _, col := range tenantTagAllColumns {
		if _, ok := insertCols[col]; ok {
			insert = append(insert, col)
		}
	}

	update := updateColumns.UpdateColumnSet(
		tenantTagAllColumns,
		tenantTagPrimaryKeyColumns,
	)
	if !updateColumns.IsNone() && len(update) == 0 {
		return 0, errors.New("dbmodel: unable to upsert tenant_tags, could not build update column list")
	}

	buf := strmangle.GetBuffer()
	defer strmangle.PutBuffer(buf)

	if len(update) == 0 {
		fmt.Fprintf(
			buf,
			"INSERT IGNORE INTO `tenant_tags`(%s) VALUES %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, insert), ","),
			strmangle.Placeholders(false, len(insert)*len(o), 1, len(insert)),
		)
	} else {
		fmt.Fprintf(
			buf,
			"INSERT INTO `tenant_tags`(%s) VALUES %s ON DUPLICATE KEY UPDATE ",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, insert), ","),
			strmangle.Placeholders(false, len(insert)*len(o), 1, len(insert)),
		)

		for i, v := range update {
			if i != 0 {
				buf.WriteByte(',')
			}
			quoted := strmangle.IdentQuote(dialect.LQ, dialect.RQ, v)
			buf.WriteString(quoted)
			buf.WriteString(" = VALUES(")
			buf.WriteString(quoted)
			buf.WriteByte(')')
		}
	}

	query := buf.String()
	valueMapping, err := queries.BindMapping(tenantTagType, tenantTagMapping, insert)
	if err != nil {
		return 0, err
	}

	var vals []interface{}
	for _, row := range o {

		value := reflect.Indirect(reflect.ValueOf(row))
		vals = append(vals, queries.ValuesFromMapping(value, valueMapping)...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, vals)
	}

	result, err := exec.ExecContext(ctx, query, vals...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: unable to upsert for tenant_tags")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodel: failed to get rows affected by upsert for tenant_tags")
	}

	return rowsAff, nil
}
